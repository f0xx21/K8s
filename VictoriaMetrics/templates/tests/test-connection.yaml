apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "victoriametrics.fullname" . }}-test-connection"
  labels:
    {{- include "victoriametrics.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "victoriametrics.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
